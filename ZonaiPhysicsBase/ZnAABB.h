#pragma once
#include <Eigen/Dense>

namespace ZonaiPhysics
{
	struct ZnAABB
	{
		// ZnAABB() : minimum(Eigen::Vector3f::Zero()), maximum(Eigen::Vector3f::Zero())
		// {}
		// ZnAABB(const Eigen::Vector3f& _min, const Eigen::Vector3f& _max) : minimum(_min),
		// 
		// Eigen::Vector3f minimum;
		// Eigen::Vector3f maximum;
	};
}